# Cowork session 交接（2026/08/04）→ 給 Claude Code

先讀 repo 的 `交接報告_Cowork.md`（完整開發規範），本文件只講今天 Cowork 做了什麼、剩什麼。

## 已完成

1. **t83 已實作完成、已 commit（`cb0be98`），但還沒 push**
   - Cowork 沙箱沒有 GitHub 憑證，push 不了。**第一件事：`git push`**，推完 GitHub Pages 測試版即為 t83。
   - 驗證已過：`node dev/check-syntax.js index-test.html` 語法 0 錯誤、`node dev/test.js` 18/18 通過。
2. **t83 內容**（APP_VERSION 第 1584 行）：
   - `cleanCreditorName()`（約 2583 行）：債權人名稱去掉序號「1)」、【銀】【私】【法】、民國日期、登序，只留本名。
   - 登序自動抓（支援 `0009-001` 與七碼 `0010000`），填 `s`，不動產頁登序欄顯示。
   - 每筆 loans 新增 `e`＝實貸估值(萬)：`defaultLoanE()` 銀/法 = Math.ceil(a/1.2)、民間 = a。金額輸入框直接顯示 e、可手改（手改設 loansManual，回寫用改後的 e，不會再 ÷1.2）。
   - 每格小字改顯示「抵押設定 XXXX萬」（原始設定金額 a）。
   - 總額回寫 p.bank/legal/private = Σe；房價評估、貸款試算直接用 `getLoanE(l)`。
   - 舊 t82 資料缺 e → `ensurePropLoans()` lazy 補；parseTa/applyTenParsed 路徑同步補。
3. 刪掉了雲端模式留下的 `push_t82.bat`、`commit_msg.txt`。
4. 確認過 index-test.html 沒有被 OneDrive 還原（原本擔心的 t81 回退沒有發生）。

## 待辦／注意

1. **push**（見上）。
2. **業主驗收重點：登序準不準**——regex 是照常見格式寫的，還沒對過真實謄本。業主丟真謄本時注意。
3. **殘留小點**：`renderTenPreview()`（約 5714 行，謄本上傳確認視窗的預覽摘要）還寫著「萬(÷1.2估)」舊字樣，不在 t83 七項規格內，問業主要不要順手改（可併入 t84）。
4. **別碰**：`index.html`、`個人GAS.gs`、`整理工具.html`、`開發手冊.md` 目前 git status 顯示 dirty，但 diff 純粹是 CRLF 換行差異（OneDrive 造成，內容零變更）。不要 commit 它們；要嘛 `git checkout --` 還原，要嘛放著。
5. 業主提醒方式照舊：測試版 Ctrl+Shift+R 強制重整（iPhone 用 Safari 無痕）。

## 分工共識（業主已同意）

- loan-crm 改版開發 → Claude Code（本機 git 一條龍）。
- 案件初評儀表板、LINE 匯出、自動歸檔、看謄本截圖 → Cowork。
