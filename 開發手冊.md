# 開發手冊 — 借款客戶 CRM 系統

> 這份文件給「接手開發的 AI / 新 code 分頁」看。讀完就能理解整個專案在幹嘛、檔案在哪、怎麼改、怎麼部署、有哪些坑。
> 業主是**民間二胎／不動產借款業務**，這套系統是他自己用來管理案源與客戶的工具。業主不是工程師，溝通用白話中文，不要丟一堆術語。
> 新分頁開始前，建議先讀這份手冊，再 `git pull` 確認本地最新。

---

## 0. 一句話總結

兩個純前端 HTML 工具，部署在 GitHub Pages：
1. **借款客戶追蹤系統**（`index.html`）— 業務的客戶/案源 CRM，資料存 Firebase。
2. **總檔整理工具**（`整理工具.html`）— 把雜亂的 Excel 名單去重、分類，整理成乾淨總檔。

---

## 1. 業務背景（一定要先懂）

- 業主做**不動產抵押借款**（二胎、民間借貸、代書案件）。
- 「案源」= 一個可能借款的客戶 + 他名下的不動產（房子）。
- 每天會從各種管道拿到名單（公司總檔、Excel、謄本），要判斷這個案子值不值得做、能借多少。
- 核心動作：**評估不動產價值 → 算可貸金額 → 追蹤客戶 → 簽約**。
- 「謄本」= 地政機關的不動產登記資料（標示部/所有權部/他項權利部），是判斷案子的關鍵文件。

### 借款相關名詞
- **他項權利部** = 房子上已經有的抵押（銀行貸款、民間借貸），決定還能再借多少。
- **預告登記** = 房子被某人卡住不能移轉，通常是民間金主放款後設的，是重要風險警訊。
- **土增稅、土地使用分區** = 評估土地價值用。
- 金額單位通常用「**萬**」。利率民間用「**分**」（月利率，1分=1%/月）。

---

## 2. 檔案與位置

| 項目 | 位置 |
|---|---|
| GitHub repo | `https://github.com/stanfordcan/loan-crm` |
| 追蹤系統線上 | `https://stanfordcan.github.io/loan-crm/` |
| 整理工具線上 | `https://stanfordcan.github.io/loan-crm/整理工具.html` |
| 本地 clone（暫存，會被清） | `C:\Users\USER\AppData\Local\Temp\loan-crm\` |
| 主程式 | `index.html`（約 4500 行，單檔含 HTML+CSS+JS）|
| 整理工具 | `整理工具.html`（約 335 行）|

> ⚠️ 本地 clone 在 temp 資料夾，可能被清掉。**GitHub 才是唯一真實來源**。要改之前先確認本地是最新：`git pull`。若 temp 資料夾不見了，重新 `git clone`。

---

## 3. 部署流程

純前端，**push 到 main 就會自動透過 GitHub Pages 上線**（約 1 分鐘生效）。

```bash
cd /path/to/loan-crm
git add -A
git commit -m "說明改了什麼"
git push
```

- 沒有 build、沒有 npm、沒有後端。直接改 HTML 存檔即可。
- 改完提醒業主：**Ctrl+Shift+R 強制重新整理**才會載到新版（瀏覽器快取很黏）。

---

## 4. 架構

### 追蹤系統（index.html）
- **Firebase Authentication**（email/密碼）登入。
- **Firestore** 是主要儲存，每個使用者一份 `users/{uid}` document。
- **localStorage**（key = `loan_crm_fb_v1`）當本地快取，靠 `uid` 隔離不同帳號。
- **載入策略（local-first）**：登入 → `loadLocal()` 先秒顯快取 → `render()` → 背景 `syncFromFirestoreIfNewer()` 比時間戳，雲端較新才覆蓋。
- Firebase 設定：projectId = `loan-crm-a60e0`（設定在 index.html 約 317 行）。

### Firestore 資料結構 `users/{uid}`
```
{
  data:       JSON字串 {clients:[...], nextId},  // 主資料
  backup:     JSON字串 {clients, nextId},        // 備份（只在非空時更新）
  backupAt:   時間戳,
  gasUrl:     共用同步 GAS 網址,
  companyUrl: 個人總檔 GAS 網址,
  updatedAt:  時間戳,
  sessionToken: 單一裝置登入控制
}
```

### 兩個 GAS（Google Apps Script）端點
1. **共用同步 GAS**（`GAS_URL`，寫死在 index.html 約 1111 行）— 舊的 Google Sheets 同步，全公司共用。
2. **個人總檔 GAS**（`COMPANY_URL`，每個使用者在 ⚙️ 設定自己填）— 讀取「自己的」Google Sheet 總檔 + 在「自己的」Google Drive 建客戶資料夾。
   - token 驗證用 `API_TOKEN = 'stanford87'`。
   - 個人 GAS 的完整程式碼見第 7 節。
   - **必須用 V8 執行階段**（不是舊的 Rhino），且要授權 Drive 完整權限（見坑 8.4）。

### 整理工具（整理工具.html）
- 純前端，無登入、無雲端。用 SheetJS 讀 Excel。
- Union-Find 演算法做去重（同電話/身分證/建號/地址視為同一人）。
- 輸出整理後的 Excel。

---

## 5. 分類系統（heat）

客戶分 6 類，**順序固定**：

| 分類 | 說明 |
|---|---|
| `待分類` | 新進來還沒判斷的 |
| `覆訪` | 要再聯絡的 |
| `追很緊` | 熱門積極追蹤（原本叫「最新資料」，已改名）|
| `新鑫` | 新鑫融資的案子 |
| `已簽約` | 成交 |
| `無效案件` | 斷聯/塗銷/無效 |

- 程式內對應在 `hOrd` / `hCls`（約 2520 行）。
- 歷史包袱：舊資料可能有 `最新資料`，`migrateHeat()` 會自動轉成 `追很緊`。
- 總檔分頁名稱 → 分類的對應在 `sheetToHeat()`。整理工具的對應在 `guessCat()`。兩邊規則要一致。

---

## 6. 主要功能與對應函式（index.html）

| 功能 | 入口/函式 | 說明 |
|---|---|---|
| 從總檔更新 | `syncFromCompany()` → `processCompanyData()` | 抓個人 GAS 的 Sheet 資料，依分頁分類匯入 |
| XLS 匯入 | `openImport()` → `doImport()` | 上傳 Excel |
| 謄本匯入 | `openTenModal()` → 解析 PDF → `applyTenParsed()` | 用 PDF.js 解析地政謄本，自動填欄位 |
| **AI 匯入** | `openAiImport()` → `parseAiImport()` → `doAiImport()` | **見第 9 節，這是給 AI 整理資料用的固定格式入口** |
| 雲端備份/還原 | `restoreBackup()` | 從 Firestore `backup` 欄位還原 |
| 建 Drive 資料夾 | `createDriveFolder()` | 呼叫個人 GAS 在使用者自己的 Drive 建資料夾 |
| 清除所有資料 | `clearAllData()` | 清本地+雲端主資料，**備份保留** |

### 客戶資料結構（clients[] 的一筆）
重點欄位：
```
{
  id, name, phone, idno, heat, note, job,
  remind, remindTime, remindNote,          // 提醒
  addr, purpose, buildNo, landNo,           // 第一間不動產的摘要欄
  mainArea, subArea, commonArea, area,      // 坪數
  bank, legal, private,                     // 貸款（萬）：銀行/法人/民間
  evalData: { ...評估分頁欄位 },             // 見下
  properties: [ {pid,label,addr,buildNo,landNo,area,bank,legal,private,...} ], // 多間不動產
  logs: [], finProofs: [], extraLoans: []
}
```
`evalData` 的 key（對應「評估分頁」）：
`district, section, landno, landzone, buildno, addr, community, buildarea, landarea, parking, parkTitle, parkval, floors, age, taxGeneral, taxSelf, taxCount, condition, resistance, jinzhai, unitprice, rate, job, need, evalNote`

---

## 7. 個人總檔 GAS 完整程式碼

業主和同事各自部署一份，貼到自己的 Google Sheet 綁定的 GAS 專案。
- 改 `SPREADSHEET_ID` 為自己的試算表 ID。
- 部署：**網頁應用程式**，執行身分=**我自己**，存取=**所有人**。
- 第一次要執行 `authorize` 函式並授權 Drive（完整權限，見坑 8.4）。

```javascript
var SPREADSHEET_ID = '試算表ID';
var TOKEN = 'stanford87';

function doGet(e) {
  var token = e.parameter.token || '', action = e.parameter.action || '';
  var callback = e.parameter.callback || 'callback';
  if (token !== TOKEN) return jsonp(callback, {status:'error', message:'invalid token'});

  if (action === 'createFolder') {
    var name = e.parameter.name || '未命名客戶';
    try {
      var parentName = '客戶資料';           // 客戶資料夾建在這個母資料夾底下
      var iter = DriveApp.getFoldersByName(parentName), parent;
      parent = iter.hasNext() ? iter.next() : DriveApp.createFolder(parentName);
      var folder = parent.createFolder(name);
      return jsonp(callback, {status:'ok', url: folder.getUrl()});
    } catch(err) { return jsonp(callback, {status:'error', message: err.toString()}); }
  }

  try {
    return ContentService.createTextOutput(callback + '(' + JSON.stringify(getSheetData()) + ')')
      .setMimeType(ContentService.MimeType.JAVASCRIPT);
  } catch(err) { return jsonp(callback, {status:'error', message: err.toString()}); }
}

function getSheetData() {
  var ss = SpreadsheetApp.openById(SPREADSHEET_ID), sheets = ss.getSheets();
  var SKIP = ['設定','config','Config','說明','Sheet1'], rows = [];
  rows.push(['電話姓名','拜訪過程','備註','建物編號','門牌地址','面積','建物所有權部','','','','','','','他項權利部','分頁']);
  rows.push(['電話/姓名欄','拜訪過程','備註','建物所有權編號','門牌地址','面積坪數','建物所有權部','','','','','','','他項權利部','分頁名稱']);
  for (var i=0;i<sheets.length;i++){
    var sh=sheets[i], nm=sh.getName(), skip=false;
    for (var s=0;s<SKIP.length;s++) if(SKIP[s]===nm){skip=true;break;}
    if(skip) continue;
    var data=sh.getDataRange().getValues();
    if(data.length<2) continue;
    for (var r=1;r<data.length;r++){
      var row=data[r], has=false;
      for (var c=0;c<row.length;c++) if(String(row[c]).trim()!==''){has=true;break;}
      if(!has) continue;
      var pad=[]; for(var c=0;c<14;c++) pad.push(c<row.length?row[c]:'');
      pad.push(nm); rows.push(pad);
    }
  }
  return rows;
}

function jsonp(cb,obj){
  return ContentService.createTextOutput(cb+'('+JSON.stringify(obj)+')')
    .setMimeType(ContentService.MimeType.JAVASCRIPT);
}

function authorize(){ DriveApp.getRootFolder(); SpreadsheetApp.openById(SPREADSHEET_ID); }
```

---

## 8. 踩過的坑（別重犯）

1. **跨帳號資料外洩**：換帳號時舊帳號資料被帶進來。根因有三層 — 舊快取沒 uid、登入/登出沒清空記憶體 `clients`、登入時自動 `syncData()` 抓到舊 GAS。解法：`loadLocal()` 嚴格比對 uid、登入登出都 `clients=[]; nextId=1;`、**移除登入自動同步**。改動這塊要非常小心。
2. **每次登入都跳「已從雲端同步」**：`saveLocal()` 和 `saveToFirestore()` 各自產生時間戳差幾毫秒。解法：同一個 `ts` 傳下去。
3. **跨裝置同步變空白**：`loadLocal()` 失敗→clients 空，但時間戳相同→不更新→空白。解法：`if (clients.length > 0 && firestoreTs <= localTs) return false;`（clients 空時強制從雲端載）。
4. **GAS Drive 權限**：部署若「執行身分」選成「使用者」會沒權限；且若 `authorize` 只呼叫 `getFoldersByName`（讀），GAS 只給 `drive.readonly`，`createFolder`（寫）就失敗。解法：appsscript.json 明確宣告 `oauthScopes: ["https://www.googleapis.com/auth/drive", ".../spreadsheets"]`，先撤銷舊授權再重授權，部署執行身分=我自己。
5. **GAS 語法**：舊 Rhino 執行階段不支援箭頭函式/展開運算子/`?.`。要嘛用 V8，要嘛全用 `var`+`for`+`.concat()`。
6. **重複偵測誤判**：曾用「同姓+同字數」判同名→所有林X都被合併。解法：只用電話/身分證/建號/地址精準比對，移除姓氏猜測。

---

## 9. AI 匯入格式（重要 — 這是 AI 協作的核心）

業主會把**謄本或案源評估表**丟給 AI（就是你），你整理成**固定 JSON**，他貼進 app「🤖 AI 匯入」一鍵建立。因為欄位跟系統 1:1 對應，匯入不需模糊比對。

### JSON 格式
```json
[{
  "name": "姓名",
  "phone": "電話",
  "idno": "身分證",
  "heat": "待分類",
  "loanPurpose": "借款用途",
  "note": "備註（預告登記、抵押權人等警訊寫這）",
  "properties": [{
    "addr": "門牌地址", "purpose": "地段(如 忠福段)",
    "buildNo": "建號", "landNo": "地號",
    "mainArea": 主建坪, "subArea": 附屬坪, "commonArea": 公設坪, "area": 總坪,
    "bank": 銀行萬, "legal": 法人/新鑫萬, "private": 民間萬
  }],
  "eval": {
    "district":"區", "section":"地段", "landno":"地號", "landzone":"使用分區",
    "buildno":"建號", "addr":"地址", "community":"社區名稱",
    "buildarea":"建物坪", "parkval":"車位坪", "landarea":"土地坪",
    "floors":"總樓層", "age":"屋齡", "taxGeneral":"土增稅一般", "taxSelf":"土增稅自用",
    "condition":"屋況", "resistance":"抗性", "jinzhai":"凶宅查詢",
    "unitprice":"行情萬/坪", "rate":"利率分", "need":"客戶需求萬",
    "job":"職業收入", "evalNote":"總價/其他備註"
  }
}]
```
- `eval` 整包選填，key 與 `evalData` 完全一致，匯入時直接帶進「評估分頁」。
- 匯入會用 **身分證 > 電話** 精準比對既有客戶：對到=更新（備註附加、不覆蓋既有）、沒對到=新增。
- 預覽可逐筆勾選、**自選分類（下拉）**、把「更新」改成「新增」。

### 建號／地號的書寫慣例（業主習慣，務必遵守）
母號-子號，**子號去掉開頭的 0；子號全 0 就只留母號**：
| 謄本原文 | 寫成 |
|---|---|
| `3204-000` | `3204` |
| `3145-0000` | `3145` |
| `3204-0001` | `3204-1` |
| `3204-0012` | `3204-12` |
地段名（如「忠福段」）放 `purpose`/`eval.section`，建號地號欄只放純號碼。

### 解讀謄本的要點
- **所有權人姓名常被遮罩成「郭＊＊」**，但「預告登記義務人」「相關他項權利」欄位常露出全名，可互相補全。
- 身分證遮罩（`H223*****6`）就留空，別亂猜。
- 坪數：主建物(層次面積)、附屬建物、共有部分(公設) 分開抓，平方公尺 ×0.3025 = 坪。
- 屋齡 = 今年 - 建築完成民國年(+1911)。
- 他項權利部每筆抵押：銀行→`bank`、租賃/融資/新鑫等法人→`legal`、私人/不動產顧問公司→`private`。金額元÷10000=萬。
- **預告登記一定要寫進 note 當警訊**，這是業務判斷風險的關鍵。

---

## 10. 跟業主互動的原則

- 用白話中文，少術語。對方是業務不是工程師。
- 改完一定講「**Ctrl+Shift+R 重新整理**」。
- 涉及 Firebase/GAS 部署，給**逐步、中文、按鈕名稱明確**的步驟。
- 動資料隔離、同步、heat 分類邏輯前要謹慎，這些都踩過坑。
- 任何 push 前確認 `git pull` 過、本地是最新。

---

# 11. 近期重大更新（2026/06/12–13）— 新接手者必讀

這段是這幾天大量新增的功能與踩過的坑，補在上面舊內容之後。

## 11.1 檔案現況
- `index.html` — 主 CRM（已成長到約 5000 行）
- `貸款試算.html` — 貸款轉貸試算表（從桌面版匯入，加了 postMessage 與 CRM 溝通）
- `整理工具.html` — 總檔 Excel 去重工具
- `開發手冊.md` — 本文件
- 版本標記：`const APP_VERSION`（在 index.html，約第 1160 行附近）。**每次 push 要更新**，顯示在標題下方。規則：**當日從 #1 開始，跨日重置**（例 `2026.06.13 #1`）。

## 11.2 個人 GAS（每人自架）目前完整指令集
`doGet` 依 `action` 分支，token=`stanford87`：
- `createFolder`：巢狀建資料夾 **`客戶資料 / 客戶名 / 門牌地址`**（每層找不到才建）。參數 `client`+`addr`（相容舊 `name`）。
- `getEvents`：讀 **所有可存取的 Google 行事曆（含共享）** `CalendarApp.getAllCalendars()`，回傳 `{title,cal,date,start,end,allday}`（Asia/Taipei 格式化）。
- 預設：回傳 `getSheetData()`（總檔）。
- `authorize()` 函式：跑一次觸發授權，內含 `DriveApp` + `SpreadsheetApp` + `CalendarApp`。
- **appsscript.json oauthScopes 需要**：`drive`、`spreadsheets`、`calendar.readonly`。
- 加新權限時 GAS 不會自動重新詢問 → 要在 appsscript.json 明確列 scope + 撤銷舊授權([myaccount.google.com/permissions]) + 重跑 authorize + 重新部署。
- ⚠️ 在編輯器直接「執行」會選到 `doGet` → 報 `Cannot read properties of undefined (reading 'parameter')`，**正常**，因為沒有網路請求 e。要測就部署後從 CRM 呼叫。

## 11.3 AI 匯入（謄本→JSON→建檔）
- 入口 `openAiImport`→`parseAiImport`→`doAiImport`。格式見第 9 節，外加 `landArea`、`extraLands`（多筆土地）、`eval` 對應評估分頁。
- 金額慣例：bank/legal/private 填**謄本設定金額（最高限額）原值**；系統匯入時 **銀行/租賃 ÷1.2** 估實貸（`recordToProps`），民間保留原值＋`privateConfirmed=false`（待確認勾選）。三條匯入路徑（總檔/AI/謄本PDF）已統一。
- 比對 `findAiMatch`：身分證→電話→建號(含 `col4BuildNo`、`normBuildNo` 正規化)→地址核心(`addrCore`)+姓氏(`surnameOf`，處理遮罩名「龔**」)→純姓名(weak)。weak 給使用者選合併/新增。
- 同建號/同地址的不動產 → `mergePropInto` 補空格，不整筆跳過。
- 建號/地號慣例：母-子，子號去前導0、全0只留母號（3204-000→3204、0354-0000→354）。

## 11.4 不動產分頁（分群卡片）
- PC 用 `#sp1.prop-tab{column-count:1}` 關掉報紙雙欄，改 `.prop-grid`（卡片）。卡片：📍位置 / 📐面積 / 🏦抵押 / 💰核定估價 / 📁Drive。
- 面積卡片 `#areaGrid` 是單一 `.fg3`，地號坪數格動態 append（`renderExtraLands`）；地號號碼在位置卡 `#landNoGrid`（5格一列）。土增稅獨立卡。
- 坪數標籤動態：`fLandAreaLabel`/`exAreaLbl{i}`，有地號→「{地號}地號面積」。
- 💰核定估價：`fEstVal`×`fLtvRate` − 既有抵押 = 可貸空間（`calcLoanRoom`）。
- 📁Drive：顯示「客戶資料/客戶名/門牌」路徑（真 URL 藏在 hidden `fDriveUrl`），建立後**立即存檔**。
- ⚠️ 手機溢出修正：grid 子項目要 `min-width:0`，`.sbody{overflow-x:hidden}`。改 grid 版面務必加 min-width:0。

## 11.5 貸款試算整合（按鈕→新視窗）
- ⚠️ **2026/06/14 更新**：`openLoanCalc` 已改開**獨立線上版** `https://stanfordcan.github.io/loan-calculator/`（另一個 repo，業主自己維護的新版）。跟 CRM **同網域** `stanfordcan.github.io`，postMessage/opener 正常。repo 內舊的 `貸款試算.html` 已不再被 CRM 使用（保留備查）。換網址前已確認新版有完整 `calc-ready`/`crm-init`/`calc-state`/`applyStateToUI` 且用相同 `origLoans` 結構，故暫存相容。
- 不動產頁「💹 貸款試算」`openLoanCalc` → `window.open(...)`（早期指向 repo 內 `貸款試算.html`）。
- 雙向溝通用 postMessage：CRM 送 `crm-init`(帶入抵押金額)、試算表回 `calc-state`(輪詢)。`貸款試算.html` 末端那段 IIFE：`host = window.opener || window.parent`。
- 狀態存 `properties[i].loanCalcState`，**改動即時自動存（2秒防抖）**。
- 若要更新試算表：替換 `貸款試算.html`，**記得補回那段 postMessage IIFE**；`applyStateToUI` 用預設值合併，舊暫存大致相容。

## 11.6 行事曆（PC 空白區 #pcEmptyState）
- `renderCalendar` 月曆格狀；側邊四格統計（今日/明日/當週/逾期）。
- 三種項目疊在一起依時間排：客戶提醒(分類色)、備忘錄(橘框📝)、Google事件(藍框📆)。
- 點日期格 → `openDayView` 當日清單；點客戶標籤 → `openEdit`（`event.stopPropagation`）。
- Google：`syncGoogleCal` 經 `COMPANY_URL?action=getEvents` 取回，快取 `GCAL_KEY`，登入自動同步。**過濾掉 CRM 自建的「📞 聯絡」事件**（`isCrmReminderEvent`）避免跟提醒重複。
- 「📅 開啟行事曆」鈕開 Google Calendar 網頁。

## 11.7 自動存 / 同步（重要！踩過資料被改壞的坑）
- `saveLocal()` = localStorage + `saveToFirestore()`（Firestore 是主雲端）。
- **備忘錄**也存進 Firestore（`saveToFirestore` 的 `memos` 欄；`saveMemos` 會觸發；登入時 `loadFromFirestore`/`syncFromFirestoreIfNewer` 載入）。
- **編輯草稿自動存**：`saveClient(silent)` + `scheduleDraftSave`（打字停1.5秒）+ closeSheet 時。
- ⚠️ **踩過的大坑**：closeSheet 原本「只要 editId 就自動存」→ 使用者只是點開客戶看一下、關掉也被重存，若欄位載入有落差就**把多筆客戶資料覆寫壞**（日期前移、時間消失）。已修為 **`closeSheet` 只在 `formDirty`(真的編輯過) 才自動存**。動自動存邏輯前務必小心，這是會損資料的地方。
- 救資料：設定 → 從雲端備份還原（Firestore `backup` 欄是非空時的快照）。
- ⚠️ **2026/06/14 修的 bug**：`saveToFirestore` 第一個 `.set()` 原本**沒 merge**＝整份覆蓋，會把 `backup`/`backupAt`/`sessionToken` 一併清掉；只要某次在「clients 為空的瞬間」存檔（剛登入/登出），備份就被永久清空 → 之後一直「沒有可用的備份」。已改成 `{merge:true}`（與 `clearAllData` 一致）。動這段務必保留 merge。
- ✅ **新增離線備份（v2026.06.14 #6）**：設定 → `downloadBackup()`「💾 下載備份檔」把 `{clients,nextId,memos,exportedAt,account,version}` 存成 `CRM備份_日期.json` 下載到裝置；`restoreFromFile()`「📂 從備份檔還原」讀回（confirm 後覆蓋，含 memos）。這是**不依賴雲端**的真‧快照，最保險，建議業主定期下載。

## 11.8 配色
低彩度專業風：冷灰中性底 + 鋼藍主色 `--accent:#3E5F92` + 柔和語意色（磚紅/鼠尾草綠/赭黃/灰紫）。全在 `:root`（含 dark mode override）。

## 11.9b 分享客戶給同事（v2026.06.14 #5）
- 編輯既有客戶 → 底部「📤 分享」鈕（`shareClient`）→ 彈窗顯示一段 JSON「分享代碼」（`clientToShareJson`），同事貼進自己 CRM 的「🤖 AI 匯入」即可建檔。
- **完全重用 AI 匯入**，同事端不用改任何東西；格式 = 第 9 節 AI 匯入格式。
- ⚠️ 金額處理：匯出時銀行/法人 **×1.2 還原**（因對方 `recordToProps` 匯入會 ÷1.2），來回抵銷；民間維持原值。整數有 ≤1萬 捨入誤差，可接受。
- `eval` 直接帶 `c.evalData`（key 完全一致）。手機 `navigator.share` 可直接叫出 LINE/訊息分享，否則「📋 複製代碼」。
- 只在 `editId` 存在（已儲存）時才給分享，避免分享到未存的暫態。

## 11.9c 地圖名單匯出 + 搜尋擴大（v2026.06.14 #7）
- 工具列「🧭 地圖名單」`openMapExport`：彈窗依分類(預設待分類/覆訪/追很緊/新鑫)列出**每筆不動產地址**(一房一列)、可逐筆勾選 → `downloadMapCsv` 產生 **UTF-8 BOM 的 CSV**（姓名/地址/電話/分類/銀行/法人/民間/用途/備註）→ Google「我的地圖」匯入，地址欄當位置、姓名當標題。
- 搜尋改用 `clientMatchesQuery(c,q)`：除原本欄位，**新增比對所有 `properties[].addr/buildNo/landNo`**（打第二間以後也找得到）。render 兩處過濾都改用它。
- ✅ **戶籍地址（v2026.06.14 #8）**：基本頁(sp0)「👤客戶資料」區加 `fCensusAddr` → 存 `c.censusAddr`；openAdd 清、openEdit 載、saveClient 存；`clientMatchesQuery` 也比對它。用途：現場找不到人時循戶籍地、判斷戶籍地是否另有不動產。注意：門牌地址在「不動產」頁(每間各自)，基本頁沒有。
- ✅ **地圖視窗搜尋框（#8）**：`mapBg` 加 `mapSearch`＋`_mapQuery`＋`mapRowMatch`，搜姓名/電話/地址；「全選/全不選」只作用在符合搜尋的列。
## 11.9d 來源批次標記（v2026.06.14 #9）
- 客戶欄位 `c.batch`（編輯頁基本資料「來源批次」`fBatch`，附 datalist 既有批次提示；save/load/openAdd 已接、`clientMatchesQuery` 也比對）。
- **批次標記工具**（工具列「🏷️ 批次」面板 →「批次標記工具」鈕 `openBatchTag`）：搜尋(姓名/電話/地址) + 「只看今日新增」(`createdAt===TODAY`) → 勾客戶 → 填批次名 → `batchTagApply` 批次寫 `c.batch`(留空=清除)、saveLocal。**刻意不碰匯入合併邏輯**，靠這工具事後圈選標記，避開同步危險區。
- **批次篩選**：工具列「🏷️ 批次」動態列出所有批次(含「（未標記）」)→ `selectedBatches`、`batchFilterPass`，render() 與 getVisibleList() 兩處都套用。
- **地圖視窗**：搜尋已含批次、清單顯示批次標籤、`mapBatchPick` 下拉「依批次快速勾選」、CSV 多一欄「批次」。
- 「是否回覆」由業主提交報表時自行判讀，**不自動算**（業主指定）。

## 11.9e 總檔多筆不動產 + 公司資料分房（v2026.06.15 #2）
- **背景坑**：`processCompanyData` 舊版把同一人多列壓成「一間扁平資料」，第二間以後只補空白→被丟掉（夫妻同址不會被誤併、客戶數不會少，流失只在房產層級）。整理工具其實有正確保留多列（`canMerge`同人不同建號不合併），所以流失點在匯入。
- **修正（已上正式版）**：`processCompanyData` 既有分支，在 `existing.companyRaw=` **被覆蓋前**，把該列用 `emptyProperty`＋欄位組成 property，**以建號(normBuildNo)/地址核心(addrHit)去重**，重複→`mergePropInto`、不同→`push` 進 `existing.properties`。順序很重要：先 migrate 再覆蓋 companyRaw，否則第一間公司資料會被後列蓋掉。
- **每筆 property 各自帶 `companyRaw`**（匯入時 `np.companyRaw=companyRaw`）；`migrateProperties` 扁平轉換時補 `companyRaw`+`col4BuildNo`。
- **公司資料分頁**：抽成 `renderCompanyPanel()`（讀 `properties[_companyPropIdx]` 的 companyRaw，fallback `c.companyRaw`）＋ `renderCompanyTabs()`（sp3 頂端 `#companyPropTabs`，≥2筆才出現不動產選擇頁籤）＋ `selectCompanyProp()`。進 sSTab(3)/openEdit 時建頁籤。
- 開發流程用過：複製 `index-test.html`（GitHub Pages 同站不同檔，測試帳號專用、資料隔離）驗證 OK 後才搬進 `index.html`，並移除測試專用的「🏠多房」鈕與 `🧪測試版` 標籤。`index-test.html` 留著當沙盒。注意 Firebase 同瀏覽器同網域一次只一個帳號（測試要換瀏覽器/無痕）。

## 11.9f 總檔格式相容 + AI土增連動 + 匯入後跳批次（v2026.06.15 #4）
- **總檔解析放寬**（`processCompanyData`，只動總檔不碰 AI 匯入）：
  - 建號：標示部沒 `A_` 代碼時，抓「`10685-000建號`」純號碼。
  - 地號/地段：共同擔保用 `[\n,，、;；]+` 多重切割（同事常用逗號擠一行）；段名與號碼黏一起也能切；每行先去重複標籤 `共同擔保(地號|建號)：`。
  - 建號/地號最後一律 `normBuildNo()`（子號全0去掉、去前導0，母號也去前導0），輸出跟 AI 匯入一致。舊的 `A_`／空格格式照舊能讀。
- **AI 匯入土增改連動**：`recordToProps` 把 `eval.taxGeneral/taxSelf`（萬）×10000 寫進 property 的 `landtaxNormal/landtaxSelf`（元，第一筆）；`mapEval` 移除 taxGeneral/taxSelf（不再直接進 evalData），房價評估改由不動產 landtax `÷10000` 連動顯示；`mergePropInto` 補 landtax 兩欄。
- **從總檔更新後自動跳批次標記**：`finalize()` 在 `added>0` 時 `openBatchTagForImport()`（開批次標記工具並預設勾「只看今日新增」），方便當下圈這批標來源。
- 開發流程：用 `index-test.html`（t5~t8）驗證後，複製覆蓋 `index.html` 並改回正式版本號、清測試字樣。

## 11.9 給接手者的話
- 每個改動：改 `index.html`(或對應檔) → 更新 `APP_VERSION` → commit → push → 叫使用者 Ctrl+Shift+R 看版本號。
- repo 在 temp（`C:\Users\USER\AppData\Local\Temp\loan-crm`），會被清；GitHub 才是真實來源，先 `git pull`。
- 動「同步/自動存/heat 分類/grid 版面」前要特別謹慎，這幾類都損過資料或爆過版。

---

# 12. 功能：☁️ 雲端預覽分頁（CRM 端已實作 v2026.06.14 #2；剩 GAS 部署）

業主想在客戶編輯頁加一個分頁，**預覽該筆不動產 Google Drive 資料夾裡的檔案**（謄本、現場照、契約等）。已跟業主討論定案。

> ✅ **CRM 端（index.html）已完成**（2026/06/14）：第 6 個分頁「☁️ 雲端」、`sSTab`/`sSTabTo` 改 0~5、`renderCloudTab()`（進分頁才打 GAS）、`openCloudFolder()`、`cloudEsc()`。進去顯示縮圖格狀（3 欄），點檔案在 Drive 開啟；沒建資料夾/沒填 COMPANY_URL 會提示。
> ⏳ **還沒做的只剩個人 GAS**：每人要把 12.3 的 `listFolder` 指令加進自己的 GAS（讀取、不用改 scope，貼上後**重新部署**即可）。GAS 沒加之前，雲端分頁會顯示「讀取失敗（個人 GAS 需先加 listFolder…）」。
> 📌 第二階段（上傳，12.5）仍未做。

## 12.1 前置背景
- 雲端資料夾結構（已上線）：**`客戶資料 / 客戶名 / 門牌地址`**（巢狀，GAS `createFolder` 已做）。
- 每筆不動產有 `driveUrl`（門牌那層資料夾的網址，存在 `properties[i].driveUrl`，hidden input `fDriveUrl`）。
- 所以雲端預覽**以「不動產」為單位**：顯示「你正在看的這筆不動產」的資料夾內容（一個客戶多筆房子，各有各的資料夾）。

## 12.2 定案的設計
- 客戶編輯頁加「**☁️ 雲端**」分頁。
- 內容：**縮圖格狀**列出該筆不動產資料夾裡的檔案（圖片/PDF 有縮圖），點檔案 → 在 Drive 開啟。
- 沒建資料夾 → 顯示「尚未建立，去不動產分頁建立」。
- 標題顯示路徑「客戶資料 / 客戶名 / 門牌」＋「📁 在 Drive 開啟」整個資料夾的鈕。

## 12.3 個人 GAS 要加的指令（listFolder）
```javascript
  if (action === 'listFolder') {
    var url = e.parameter.url || '';
    try {
      var id = (url.match(/folders\/([A-Za-z0-9_-]+)/) || [])[1];
      if (!id) return jsonp(callback, {status:'error', message:'invalid url'});
      var folder = DriveApp.getFolderById(id);
      var it = folder.getFiles(), out = [];
      while (it.hasNext()) {
        var f = it.next();
        out.push({
          name:  f.getName(),
          id:    f.getId(),
          mime:  f.getMimeType(),
          url:   f.getUrl(),
          thumb: 'https://drive.google.com/thumbnail?id=' + f.getId() + '&sz=w300'
        });
      }
      return jsonp(callback, {status:'ok', files: out});
    } catch(err) { return jsonp(callback, {status:'error', message: err.toString()}); }
  }
```
- 權限：`drive` scope 已授權，不用再改 scope，加完**重新部署**即可。
- 縮圖 `drive.google.com/thumbnail?id=...` 需要使用者瀏覽器有登入 Google（通常都有）。

## 12.4 CRM 端要做的
1. 客戶編輯頁加第 6 個分頁 `☁️ 雲端`：
   - HTML 加 `<button class="stab" onclick="sSTab(5,this)">雲端</button>` 和 `<div id="sp5">`。
   - `sSTab`/`sSTabTo` 目前處理 0~4，要**改成 0~5**（`[0,1,2,3,4,5].forEach(...)`）。
2. 進入分頁時：取目前 `properties[curPropIdx].driveUrl`，若有 → `jsonpFetch(COMPANY_URL+'?action=listFolder&token='+API_TOKEN+'&url='+encodeURIComponent(driveUrl))`，回來渲染縮圖格狀（每個 `<img src=thumb>` + 檔名，點 → `window.open(f.url)`）。若無 driveUrl → 提示去不動產分頁建立。
3. 注意：`sSTab(4)` 那種「進分頁重新渲染」的模式可參考（房價評估就是進分頁時 `renderPriceEval`）。雲端分頁同理：進去才 fetch（不要每次 render 都打 GAS）。

## 12.5 第二階段：手動上傳（已實作 v2026.06.14 #4）
- ✅ ☁️雲端分頁加「⬆️ 上傳」鈕（`uploadCloudFile`）：選圖片/PDF → 讀成 base64 → **POST（`mode:'no-cors'`）** 給個人 GAS `uploadFile` → 存進該筆不動產資料夾 → 2.5 秒後自動重抓縮圖。
- 個人 GAS 已加 `doPost`／`uploadFile`（見 repo `個人GAS.gs`）：收 JSON `{token,action:'uploadFile',url,filename,mime,data(base64)}`，`Utilities.base64Decode`+`newBlob`+`folder.createFile`。只用既有 Drive 寫入權限，**不用重新授權**，重新部署即可。
- ⚠️ no-cors 讀不到回應，所以 CRM 不判斷成功與否，改用「上傳後重抓 listFolder」確認；若沒出現多半是 GAS 沒部署新版或 token 不符。
- 檔案上限約 25MB（base64 會放大約 1.33 倍，過大 GAS 會逾時）。
- （未做）試算表一鍵截圖上傳：需動到 loan-calculator（別的對話維護），暫不做。

## 12.6 實作提醒
- 加分頁會動到 `sSTab`/`sSTabTo`（目前寫死 0~4），改的時候確認 5 個舊分頁都沒壞。
- listFolder 是讀取、無副作用，相對安全。
- 一樣：改完更新 `APP_VERSION` → commit → push → 提醒業主 Ctrl+Shift+R。

---

# 13. 2026/06/16 session — 新接手者必讀

## 13.0 版本現況
- 正式版 `index.html`：`APP_VERSION = '2026.06.16 #5'`（已上線）。
- 測試版 `index-test.html`：`🧪測試版 t20`。本地 clone 改在桌面 `C:\Users\USER\OneDrive\桌面\claude\loan-crm`（不是手冊舊寫的 temp）。

## 13.1 已上正式版 #5
- **日期 bug（重大）**：Google 試算表同步把提醒日期少一天、時間消失。根因＝`normalizeDate`/`normalizeTime`（約 1395 起）把試算表回傳的 UTC ISO 直接切前10碼。已改成含時區(Z/±hh:mm)就用**本地時間**換算日期、時間欄補 ISO 解析。
- **移除「🔄 同步」按鈕**：那顆舊 Google 試算表同步(`syncData`)正是日期 bug 元兇；跨裝置靠 Firebase 自動同步(時間戳比較，`syncFromFirestoreIfNewer`)即可，加上單裝置登入鎖(`claimSession`/`forceLogout`)，不需要它。一併移除工具列顯示設定裡的 `syncBtn` 項。
- **雲端備份永遠空白 bug**：備份原跟主資料塞同一份 Firestore 文件→撐爆 1MB 默默失敗。改存獨立子文件 `users/{uid}/backups/latest`；寫入失敗會跳訊息(`backupWarned` 旗標)。`restoreBackup` 先讀新文件、相容舊欄位。
- **B（嚴格版）**：`processCompanyData` 略過「無效案件＋無電話/身分證/建號」的新列。⚠️ 正式版是**嚴格版**(要求連建號都沒有)，尚未換成測試版 t19 放寬版。

## 13.2 在測試版 t20、待驗證後搬正式版
- **地圖**：業主決定**不做內建地圖**。已移除 Google 地圖檢視／排順路／定位／Google 金鑰設定(t16)。**只留「地圖名單匯出 CSV」**(`downloadMapCsv`，給 Google 我的地圖)。
- **公司資料多欄位**：匯入存總檔**整列 25 欄** `companyRaw.cols`；公司資料頁(`renderCompanyPanel` + `COMPANY_COL_LABELS`)改**照總檔欄位順序(電→金主門牌)完整顯示**。重匯時房產層 `companyRaw` 改成**永遠刷新**(原本 `if(!dup.companyRaw)` 害新欄位進不去)。
- **戶籍地址帶入**：總檔第 7 欄「所有權人住址」→ 基本頁 `censusAddr`(新增直接帶、既有空白才補)。
- **📋 名單整合面板**(取代工具列「🧭地圖名單」；批次面板的「批次標記工具」子鈕也併入)：勾客戶(**預設不勾**)→篩選(分類晶片＋**批次當篩選**)→三動作 🏷️標批次/🗺️地圖名單CSV/📤公司格式匯出。**響應式**(電腦置中視窗+篩選常駐 / 手機全螢幕+收合+底部固定，`#ltPanel` media query)。函式在 `downloadMapCsv` 之後 `// ── 📋 名單整合面板` 整段。
- **📤 公司格式匯出**(`ltExportCompany`)：一人一列、25 欄；備註用 `c.note`、拜訪過程由 `c.logs` 重建成民國格式(`logsToProcess`)；多房取主要那筆；沒重匯過(無 cols)的只出基本欄。
- **🧹 清除無效空資料**(⚙️設定內，`cleanupInvalidEmpty`)。
- **B（放寬版 t19）**：改成 `heat==='無效案件' && !phone && !idno`(**不看建號**)；因那些無效人其實有建號(標示部)，嚴格版漏掉。

## 13.3 ⚠️ 沒解決的 BUG（最優先）
**總檔更新後，那 29 個無效案件的人還是被當「今日新增」跳出來**，即使 t19 放寬了 B。線索：
- 匯入後自動跳的「批次標記工具」(`openBatchTagForImport`)顯示**所有 `createdAt===TODAY`** 的人，不只本次新增 → 他們出現**不等於** B 失效，可能是今天稍早(B 修好前)就建立、留著。
- **正確測法**：先把這 29 個**全刪乾淨**→**只匯入一次**→看會不會回來。會＝B 真沒擋(在 else 分支 B 那行前 `console.log(heat,phone,idno,name)`，查是否 idno 被解析成非空、或走了 新鑫/「rawCell空+buildNo合併」/fuzzy 等非 else 路徑)。不會＝先前殘留。
- 正式版 B 還是嚴格版，修好後 B＋清除一起搬正式版。

## 13.3b 另外兩個待查（2026/06/16 業主回報）
- **備註遺失(資料遺失，高優先)**：業主說「王恩興」的備註不見了。一般客戶匯入時 `c.note` 是保留的(existing 分支不動 note)，**但「新鑫」分支會 `existing.note = mergeXinNote(...)`(index-test.html:2513) 改寫備註**——格式對不上時可能吃掉手寫備註。也可能是「無法比對的人被當新人重建」(同 13.3 根因)。下一手：確認王恩興是不是新鑫名單；檢查 mergeXinNote 的保留邏輯；確認 unmatchable 重建時 finalize 去重是保留舊的(有備註)還是新的。
- **門牌的「區」錯成北投**：業主說幾個信義的跑到北投。根因＝**總檔「門牌」欄(col4)本身就填成「台北市北投區 信義路六段…」**，而標示部(col3, 謄本)是「臺北市 信義區 …」才對。CRM 照實匯入 col4。可考慮：匯入 addr 時改**優先用標示部的縣市+區**(謄本權威)覆蓋門牌欄的區。危險區、走測試版。

## 13.4 GAS 更新（業主+同事都要做）
- repo `個人GAS.gs` 的 `getSheetData` 已把 `for(var c=0;c<14;...)` 改成 `<25`，才會送出第 14～24 欄(限制登記事項/權利人/金主門牌…)。**每人重新部署**(管理部署作業→編輯→新版本，網址不變)。沒更新前公司資料頁只看得到前 14 欄。

## 13.5 互動原則（不變）
- 白話中文、少術語、回覆精簡；改完一定講 Ctrl+Shift+R；動危險區(同步/自動存/heat/grid/`processCompanyData`/多房合併)前請業主先「💾下載備份」、走測試版。

## 13.6 待做：把「批次篩選」併進「縣市」按鈕（業主指定，交接給接手系統）
**緣由**：「標批次」已移到工具列「📋 名單」整合面板，所以工具列那顆獨立「🏷️ 批次」按鈕就只剩「篩選主清單」一個作用，跟「🗺️ 縣市」篩選同類 → 業主要把兩顆合成一顆，工具列更乾淨。

**規格**：
- 工具列「🗺️ 縣市」按鈕(`cityFilterBtn`)改名「🗺️ 篩選」，它的下拉面板(`cityFilterPanel`)在原本的縣市勾選下方，**加一段「依來源批次篩選」**(把原 `batchFilterPanel` 裡的 `#batchCheckboxes` 整段搬進來，含「清除」`clearBatchFilter()` 與底下「標記批次請用📋名單」提示)。
- **移除**獨立的「🏷️ 批次」按鈕(`batchFilterBtn`)與其面板(`batchFilterPanel`)。
- `toggleCityFilter()` 打開面板時要 `renderBatchCheckboxes()`(讓批次清單填上)。
- 徽章合計：`cityFilterCount` 顯示「縣市數＋批次數」總和(寫個 `updateFilterBadge()`，`onCityChange`/`onBatchChange` 都呼叫它)。
- ⚠️ `onBatchChange` 原本寫 `document.getElementById('batchFilterCount').textContent=...`——`batchFilterCount` 會被移除，**改成呼叫 `updateFilterBadge()`**，否則 null 會報錯擋掉批次篩選。
- 外點關閉面板的 handler 引用 `batchFilterBtn/batchFilterPanel`，有 `if(bbtn&&bpanel)` 防呆，移除後是 null→跳過，安全；殘留的 `toggleBatchFilter()` 變死碼、無人呼叫，可留可刪。
- 一樣：改 `index-test.html`(或正式版)→更新 `APP_VERSION`→commit→push→提醒業主 Ctrl+Shift+R。

> 註：前一個對話(本手冊作者)本來在做這項、已有未提交的草稿，但因偵測到**另一個系統正平行在同一檔上動工(t22「同身分證合併」)**，為避免兩邊覆蓋，**已 `git restore` 丟棄那份未提交草稿**，改用這份規格交接。接手系統請在**最新 `index-test.html`** 上重做這項。

---

# 14. 2026/06/17 session — 新接手者必讀（大量更新，已上正式版）

## 14.0 版本現況
- **正式版 `index.html`：`2026.06.17 #4`**，已上線 https://stanfordcan.github.io/loan-crm/
- 測試版 `index-test.html`：🧪測試版 **t53**（內容與正式版 #4 完全一致，只差版本字串）。
- 本 session 從 t25 一路做到 t53，整批已搬上正式版 #4。發版規則不變：當天 #1 起跳、跨日重置；改 index.html→更新 APP_VERSION→git pull→commit→push→提醒業主 Ctrl+Shift+R。
- **此 session 13.3/13.3b/13.6 的待辦都已完成並上線**（無效案件改只看電話、新鑫蓋分類/備註修正、門牌區修正、批次併進篩選…）。

## 14.1 這次做了什麼（依主題）
1. **無效案件(13.3)**：B 略過/🧹清除改成「無效＋沒有任何電話(本人+債電)」，**不看身分證**(謄本本來就帶屋主身分證，那不是聯絡方式)。`cleanupInvalidEmpty`。
2. **新鑫匯入(13.3b-1)**：曾把業主手動分的「追很緊/覆訪」蓋成新鑫、且備註被 mergeXinNote 吃掉。改 `keepHeats` 保護追很緊/覆訪；備註保護。
3. **手動分類鎖 `heatManual`**：在 CRM 親手分類(非待分類)存檔→`saveClient` 設 `heatManual=true`；三條匯入路徑(主分支 `canUpdateHeat`、新鑫 `keepHeats`、模糊審查 merge)都加 `!heatManual` 判斷，鎖住的分類匯入絕不覆蓋。
4. **門牌「區」修正(13.3b-2)**：總檔門牌欄(col4)的區常被人工填錯(信義填成北投)；用標示部(col3)「縣市 區 段 建號」那行的權威縣市+區，只在「區」不一致時覆蓋(縣市/街道保留)。`fixAddrDistrict`。
5. **去重不誤刪同名不同人(t28)**：`finalize` 去重 key2 原本「姓名+地址前10字」，遮罩名縮成單姓(陳)+前10字只到街路→同街不同人被誤刪。改用**完整正規化地址**。`normalizeAddr` 加去句點。
6. **批次視窗只看本次新增(t29)**：匯入前記 `_startNextId`，id≥它的才是本次新增；`openBatchTagForImport(newIds)`+`_importNewIds`，批次標記視窗只顯示本次新增的人。
7. **○先生/小姐(t30)**：`genderFromId`(身分證第二碼 1男2女，遮罩也判得出)、`dispName(c)`(沒全名→姓+先生/小姐)。套在清單/客戶頁標題/日曆/名單/疑似重複/謄本選單/批次清單；搜尋 `clientMatchesQuery` 也比 dispName。
8. **流水編號 `serial`(t37-38)**：每人唯一隨機6位數、給了就鎖死(`ensureSerials` 只補沒有的)。在載入/總檔finalize/saveClient/AI匯入都呼叫。顯示在「出生年月日」那行右邊(唯讀 `fSerial`)、清單卡片🔢、可搜尋反查。存進 toRow/fromRow 的 rich JSON(欄34)避免從試算表重建遺失。
9. **遮罩身分證+戶籍地帶入(t26)**：`idnoFromOwner`(完整優先,否則遮罩版只取緊接姓名前那組)；`isFullId` 守門(只有完整身分證才拿來比對/合併,遮罩版只顯示)；戶籍地 `censusFromCompanyRaw`(col7→所有權部第二行)。
10. **聯絡紀錄解析重寫(t32) `parseVisitLogs`**：逐行解析+日期驗證(月1-12/日1-31/時分)、丟純數字雜訊、四格式(民國斜線/西元8碼MMDDHHMM/民國7碼/西元MMDD+可選HHMM)。**未來日期根治(t48)**：`yearForMD`—MMDD補年份時月日比今天晚就用去年(匯入幾次都不跑到未來)。
11. **🔄 一鍵套用新規則到既有客戶 `applyNewRulesToExisting`/`runApplyNewRules`**(⚙️設定)：從客戶已存的 companyRaw 重算，補遮罩身分證/戶籍地、**門牌空白從標示部抓門牌**(`bestDoor`：標示部門牌行→退用所有權部住址)、修門牌區、**新鑫批次標頭(1150522_資料(N))搬進 `batch` 欄**、重洗聯絡紀錄(壞日期清空/未來日期撥回一年/純數字雜訊)。先 dryRun 顯示筆數 confirm。
12. **🧹 清理髒資料 `cleanupDirtyData`**(⚙️設定)：刪「姓名/地址」標題列假客戶、清地址裡的 Excel 公式片段(如 +E54:P54,用`[0-9]`只認半形)、清「身分證(+數字)」錯放成地址的(順便補民國生日)、清備註裡的 ISO 系統時間戳(2026-05-21T16:00:00.000Z)。先列清單 confirm。
13. **新鑫批次=來源批次(t51)**：新鑫匯入時批次標頭進 `batch` 欄、個人備註留 note(改法比舊 mergeXinNote 安全)。既有的靠 🔄 搬。
14. **待分類分頁(t27)**：`sheetToHeat` 補 `待分類/未分類/待處理→待分類`(原本掉到預設追很緊)。
15. **🗺️篩選(t25)**：工具列「縣市」改名「篩選」、批次篩選併進同面板、移除獨立批次鈕；`updateFilterBadge` 顯示縣市+批次合計。
16. **📋名單面板**：只留三顆—🏷️標批次/📤公司格式/📨通知單名單(Word合併列印)。移除地圖名單/網頁列印(函式 `ltMapCsv`/`ltPrintNotice` 留著沒刪、無人呼叫)。
17. **AI匯入對齊(t50)**：`findAiMatch` 身分證比對加 `isFullId` 守門；`doAiImport` 吃 JSON 的 `censusAddr`、結尾 `ensureSerials`。
18. **設定面板(t53)**：`#urlSettingsBg` 的 immodal 加 `max-height:85vh;overflow-y:auto`、h3 sticky；整理區三顆按鈕說明收成精簡兩行。

## 14.2 通知單（包裏信件投遞通知單）— Word 合併列印
- 業主有 Word 範本 `包裏信件通知單.docx`(2×2表格、一頁4格、標楷體)。**決定走 Word 合併列印(Mail Merge)**，不在 CRM 重畫(避免跑版)。
- CRM 端：📋名單勾客戶→**📨 通知單名單(Word合併列印)** `ltExportNoticeData`→匯出 CSV，欄位＝合併欄位名：流水編號/姓名/地址/區/路/段/巷/弄/號/樓/樓別/投遞姓名/投遞電話。地址用 `noticeAddrParts` 拆解(已處理全形數字)。投遞姓名/電話列印前 prompt 詢問、記 localStorage。
- Word 端(業主一次性設定)：開範本→郵件→開始合併列印→選 CSV(UTF-8)→各空格插入合併欄位→第2~4格開頭加「Next Record」→完成並合併→**另存 PDF**(業主無印表機,用「Microsoft Print to PDF」或另存 PDF)。
- **狀態：業主明天(06/18)到公司實測整套寄信流程**。`noticeSlip`/`ltPrintNotice`(網頁列印版)仍在程式裡但按鈕已移除，保留備用。

## 14.3 ✅ Firestore 安全規則（2026/06/17 已確認安全，無需更動）
**業主已貼出現行規則、確認正確安全**：
```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```
＝必須登入且只能讀寫自己帳號(`users/{uid}`，含 backups 子集合)。沒登入讀不到、別帳號互看不到、其他路徑預設拒絕。資料路徑全在 `users/{uid}`，此規則完整涵蓋。**程式碼公開不影響資料安全**(client 端程式/Firebase config 公開是正常的，防線在這條規則)。以下為當初的背景說明：
- 業主一度擔心「放 GitHub 別人看得到資料」。已澄清：**客戶資料在 Firestore、不在 GitHub**；GitHub/瀏覽器只看得到程式碼(含 Firebase config，這是正常的)；做成 Firebase Hosting 或 App 都藏不住 client 端程式碼。
- **真正保護資料的是 Firestore Security Rules**。資料路徑全在 `users/{uid}`(及 `users/{uid}/backups/latest`)。應確認規則為：
  ```
  match /users/{uid}/{document=**} { allow read, write: if request.auth != null && request.auth.uid == uid; }
  ```
- 業主說「下次再做」。**下次接手請優先請業主把 Firebase 主控台的現行規則貼出來確認**——若是 `if true` 或 `if request.auth != null`(沒比對 uid，同事互看得到)就要改。

## 14.4 其他狀態 / 提醒
- **資料清理**：業主已自行跑過清理(回報「處理好了」)。⚙️設定裡有三顆整理工具(🔄/🧹清理髒資料/🧹清除無效空資料)，動資料前都先「💾下載備份」、有 confirm。
- **GAS 25 欄**(13.4)：repo `個人GAS.gs` 的 `getSheetData` 已改 `<25`，但**業主與同事仍需各自重新部署**才會送出第14~24欄。沒更新前公司資料頁只看得到前14欄。(本 session 未再確認是否已部署)
- **危險區照舊**：`processCompanyData`/新鑫分支/多房合併/`finalize`去重/heat 分類/`saveLocal`(同時寫本地+Firestore)。動前下載備份、走測試版。
- **互動原則不變**：白話中文、回覆精簡；業主是民間二胎/不動產借款業務、非工程師。

## 14.5 本機開發環境（2026/06/18 裝好，接手者可用）
業主同意安裝，已用 winget(user scope) 裝好：
- **Python 3.12.10**：`C:\Users\USER\AppData\Local\Programs\Python\Python312\python.exe`（注意 `python` 指令會被 WindowsApps 假 stub 攔截，要用完整路徑）。可起靜態伺服器：`python.exe -m http.server 8765`（在 loan-crm 目錄）。
- **Node v24.16.0**：`C:\Users\USER\AppData\Local\Microsoft\WinGet\Packages\OpenJS.NodeJS.LTS_*\node-*-win-x64\node.exe`。
- ⚠️ Claude Preview MCP(`preview_start`)在此環境**啟動失敗(exit 9009，疑似它會把 runtimeExecutable 後加 .cmd)**，用不了；改用下面兩招驗證。
- **語法檢查(push 前必跑)**：`node dev/check-syntax.js index-test.html`（也檢查 index.html）→ 把每段內嵌 script 用 vm 編譯，回報語法錯誤。本 session 兩檔皆 0 錯。
- **回歸測試(push 前建議跑)**：`node dev/test.js index-test.html` → 從 HTML 抽真函式跑，驗 18 項核心邏輯(性別/○先生小姐、isFullId、地址拆解、聯絡紀錄解析、未來日期防護(相對日期)、去重/合併/房產合併)。不需備份檔、時間相關用相對日期。改完核心邏輯後跑一次。
- **真資料壓力測試(深測)**：把備份 JSON 餵進函式跑+丟邊界輸入(null/數字/物件/超長/emoji)。本 session 跑過 370 真客戶+三大塊(去重/通知單地址/整理工具🔄🧹)共數十項全過、0 crash、0 未來日期、0 不可能日期；唯一抓到 `normalizeAddr` 對非字串會 crash→已加 String() 防呆(t54)。腳本在 `%TEMP%`(stress-test.js/suite.js)，需要時請接手者依 dev/test.js 模式重建並指向最新備份。
- 要跑整個 App(含登入/Firebase)：可 `python -m http.server` 服務後，用瀏覽器或 Chrome MCP 開 `localhost:8765/index-test.html`；純函式測試不需登入。
